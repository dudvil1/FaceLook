module.exports = class PostUser {
    constructor(userId, username, email) {
        this.post_user = {
            "name": "user"
        };
        this.userId = userId;
        this.username = username;
        this.email = email;
    }
}