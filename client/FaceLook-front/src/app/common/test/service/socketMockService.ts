import { ISocketService } from '../../service/socket.service';
import { IPost } from '../../model/post';

export class SocketMockService implements ISocketService{
    addPost() {
        
    }
    updateLike(post: IPost) {

    }
}